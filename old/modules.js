// Modules in NodeJS

// CommonJS every file is a module (by default)
// Modules - Encapsulated code (Only share Minimum)

const names = require('./names').default;
const sayHi = require('./utils').default;

//sayHi('Minal')
sayHi(Dhiraj)
sayHi(Arvind)